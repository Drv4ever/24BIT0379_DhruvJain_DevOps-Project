define command {
    command_name    check_http_port
    command_line    /opt/nagios/libexec/check_http \
                    -H $HOSTADDRESS$ -p $ARG1$ -u / -s "ABC Technologies"
}

define command {
    command_name    check-host-alive
    command_line    /opt/nagios/libexec/check_http \
                    -H $HOSTADDRESS$ -u / -f follow -t 5
}
